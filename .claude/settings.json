{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "permissions": {
    "allow": [
      "Bash(gcloud compute ssh instance-template-20260309-20260309-113128-a *)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Read-only diagnostic SSH into the project's GCP VPS (instance instance-template-20260309-20260309-113128-a, zone asia-southeast1-a, domain thhflow.com) is pre-authorized for the user and any subagents acting on their behalf. Allowed remote commands (run via gcloud compute ssh ... --command=\"...\"): ls/cat/grep, pm2 logs vcw-server, sqlite3 prisma/dev.db SELECT queries, curl http://127.0.0.1:*, pgrep, and running the existing diagnostic script node /opt/vcw/app/server/test-recaptcha-vps.mjs. Mutating commands (pm2 restart, pkill, rm, npm install, git pull) still require user confirmation."
    ],
    "environment": [
      "$defaults",
      "Project VPS context: the GCP instance instance-template-20260309-20260309-113128-a (zone asia-southeast1-a, domain thhflow.com, app dir /opt/vcw/app, PM2 process vcw-server) is the user's own production server. The user (hoanghuycptqt@gmail.com) is the sole operator. Diagnostic SSH access via gcloud compute ssh ... --tunnel-through-iap --command=\"...\" is sanctioned by the user for debugging the Video Creator Workflow application, particularly the Google Flow reCAPTCHA pipeline."
    ]
  }
}
