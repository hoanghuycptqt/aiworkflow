# ═══════════════════════════════════════
# PRODUCTION / VPS ENVIRONMENT
# Copy this to .env on your VPS server
# ═══════════════════════════════════════

DATABASE_URL="file:./dev.db"
JWT_SECRET="CHANGE-THIS-TO-A-STRONG-SECRET"
JWT_EXPIRES_IN="7d"
PORT=3001
REDIS_URL="redis://localhost:6379"
UPLOAD_DIR="./uploads"

# Chrome (Linux server)
CHROME_PATH=/usr/bin/google-chrome

# Telegram Bot — WEBHOOK mode for production
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_MODE=webhook
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/api/telegram/webhook

# AI Provider
TELEGRAM_AI_PROVIDER=gemini
GEMINI_API_KEY=your-gemini-api-key
OPENROUTER_API_KEY=

# Google Flow Python broker (invisible_playwright Firefox)
# Runs as systemd service vcw-flow-broker — see python-broker/deploy/
FLOW_BROKER_URL=http://127.0.0.1:8002
# BROKER_AUTH_TOKEN=  # optional bearer secret if broker.env sets one
